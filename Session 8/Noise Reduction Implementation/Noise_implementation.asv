pic = 'test1.jpg'
A = imread(pic);

%get the filter
h_gaussian = fspecial('gaussian');
h_gaussian = fspecial('everage');

%Perform filtering 
B_Gaussian = imfilter(A,h_gaussian);
B_average = imfilter(A, h_average);

%Perorm Media filtering
A_gray = rgb2gray(A);
B_median =  medfilt2(A);

%plott